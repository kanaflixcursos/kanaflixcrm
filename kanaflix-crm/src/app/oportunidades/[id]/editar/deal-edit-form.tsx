"use client";

import { useActionState } from "react";
import { LoaderCircle, Save, Trash2 } from "lucide-react";
import { deleteDeal, updateDeal, type UpdateDealState } from "../../actions";

const initialState: UpdateDealState = {};

type Deal = { id: string; title: string; contact_id: string | null; stage_id: string | null; amount: number | string; expected_close_date: string | null };

export function DealEditForm({ deal, contacts, stages }: Readonly<{ deal: Deal; contacts: { id: string; full_name: string }[]; stages: { id: string; name: string }[] }>) {
  const [state, formAction, isPending] = useActionState(updateDeal, initialState);
  return <div className="space-y-6"><form action={formAction} className="rounded-3xl border border-border bg-surface p-6 shadow-sm sm:p-8"><input type="hidden" name="dealId" value={deal.id} /><div className="grid gap-5 sm:grid-cols-2"><label className="block space-y-2 sm:col-span-2"><span className="text-sm font-medium">Oportunidade</span><input name="title" required defaultValue={deal.title} className="h-12 w-full rounded-2xl border border-border bg-surface px-4 text-sm" /></label><label className="block space-y-2"><span className="text-sm font-medium">Contato</span><select name="contactId" required defaultValue={deal.contact_id ?? ""} className="h-12 w-full rounded-2xl border border-border bg-surface px-4 text-sm"><option value="" disabled>Selecione um contato</option>{contacts.map((contact) => <option key={contact.id} value={contact.id}>{contact.full_name}</option>)}</select></label><label className="block space-y-2"><span className="text-sm font-medium">Etapa</span><select name="stageId" required defaultValue={deal.stage_id ?? ""} className="h-12 w-full rounded-2xl border border-border bg-surface px-4 text-sm">{stages.map((stage) => <option key={stage.id} value={stage.id}>{stage.name}</option>)}</select></label><label className="block space-y-2"><span className="text-sm font-medium">Valor estimado</span><input name="amount" type="number" min="0" step="0.01" defaultValue={deal.amount} className="h-12 w-full rounded-2xl border border-border bg-surface px-4 text-sm" /></label><label className="block space-y-2"><span className="text-sm font-medium">Previsão de fechamento</span><input name="expectedCloseDate" type="date" defaultValue={deal.expected_close_date ?? ""} className="h-12 w-full rounded-2xl border border-border bg-surface px-4 text-sm" /></label></div>{state.error && <p className="mt-5 rounded-2xl bg-brand-soft px-4 py-3 text-sm">{state.error}</p>}<div className="mt-8 flex justify-end border-t border-border pt-6"><button type="submit" disabled={isPending} className="inline-flex h-12 items-center justify-center gap-2 rounded-2xl bg-brand px-5 text-sm font-medium text-brand-foreground transition-colors hover:bg-brand-hover disabled:opacity-60">{isPending ? <LoaderCircle className="animate-spin" size={18} /> : <Save size={18} />}Salvar alterações</button></div></form><form action={deleteDeal} onSubmit={(event) => { if (!window.confirm("Excluir esta oportunidade? As atividades ligadas a ela também serão removidas.")) event.preventDefault(); }} className="flex flex-col gap-4 rounded-3xl border border-danger/25 bg-surface p-6 shadow-sm sm:flex-row sm:items-center sm:justify-between sm:p-8"><input type="hidden" name="dealId" value={deal.id} /><div><p className="text-sm font-medium">Excluir oportunidade</p><p className="mt-1 text-sm leading-6 text-muted-foreground">Essa ação remove também as atividades diretamente vinculadas a esta negociação.</p></div><button type="submit" className="inline-flex h-11 shrink-0 items-center justify-center gap-2 rounded-2xl border border-danger/35 px-4 text-sm font-medium text-danger transition-colors hover:bg-danger hover:text-white"><Trash2 size={17} />Excluir</button></form></div>;
}
